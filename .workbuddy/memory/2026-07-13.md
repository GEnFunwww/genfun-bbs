# 2026-07-13

## GENFUN论坛 搭建
- **html**: `C:\Users\Administrator\Desktop\BBS\GENFUN论坛.html`
- **server**: `C:\Users\Administrator\Desktop\BBS\server.js`
- **端口**: 3456
- **数据目录**: `C:\Users\Administrator\Desktop\BBS\genbbs/`

### 已完成功能
1. 邮箱/手机号（2选1）登录注册
2. 帖子列表（分类筛选、关键词搜索、分页）
3. 帖子详情 + 回复
4. 发帖（含附件上传）
5. 大文件上传（<100MB 直接上传带进度条，>100MB 自动分块上传 5MB/块）
6. 暗色主题，响应式布局
7. 数据存储：genbbs/users.json + genbbs/posts.json + genbbs/uploads/
8. 黑白主题切换（🌓 按钮，localStorage 记忆，蓝色/青色点缀）
9. 右下角圆形FAB加号按钮（登录后在首页显示）
10. 邮箱/手机号验证码（6位数字，5分钟过期，60秒冷却）
11. 修复：添加 GET / 路由，统一通过 http://localhost:3456 访问（避免 file:// CORS 问题）
12. 接入 nodemailer 发送邮件（163 SMTP），但授权码认证失败（535 Error），已添加回退机制：邮件发送失败时自动返回验证码到前端显示
13. 163邮箱配置：smtp.163.com:465 SSL，user=Rr_052052052052@163.com，授权码=PBWqF32Vn7ZtVTV4（认证失败，待用户确认）

### 技术栈
- 后端：Express + multer + cors
- 前端：单文件 HTML + 原生 JS（SPA）
- 存储：JSON 文件
- 认证：SHA256 密码 + Token 会话

### 启动方式
`cd C:\Users\Administrator\Desktop\BBS && node server.js` 或 pm2 守护
访问 http://localhost:3456

### 管理后台 (19:30-19:42 新增)
14. 完整管理后台仪表盘：📊仪表盘（8项统计）、📝帖子管理（批量删除/置顶）、👥用户管理（封禁/解封/角色切换）、⚙系统设置（站点名称/公告/注册开关）
15. 新增后端API：GET /api/admin/stats、GET /api/admin/posts、POST /api/admin/posts/:id/pin、POST /api/admin/posts/batch-delete、POST /api/admin/users/:id/ban、GET/POST /api/admin/settings
16. 用户封禁机制：banned 字段，被封禁用户无法登录，会话自动清除
17. 帖子置顶：pinned 字段，置顶贴排在最前
18. T用户 密码=T123456，role=admin
19. 修复导航按钮无点击事件（加了onclick），修复管理后台 page-admin 内联 style="display:none" 覆盖 .active 类导致面板无法显示
20. 新增关注系统：POST /api/follow、GET /api/follow/:userId、GET /api/user/:id/profile、GET /api/user/:id/fans|following（follows.json存储关系）
21. 新增用户主页：点击帖子/回复作者头像昵称进入主页，显示粉丝/关注/帖子数，关注/取关按钮
22. 新增多配色方案：🎨按钮弹出面板，6种预设（蓝青/翠绿/红橙/紫/粉/青绿）+ 自定颜色选择器，存储localStorage
23. 修复路由顺序bug：404 catch-all 在关注路由之前导致新API返回HTML。移至末尾解决。
24. 云端部署准备：创建Dockerfile、fly.toml（东京节点/3GB持久卷/HTTPS）、.dockerignore、.gitignore
25. 修改server.js：DATA_DIR支持环境变量（云端指向持久卷/data/genbbs，本地默认genbbs/）
26. flyctl安装受阻（GitHub被墙），需用户开VPN手动安装后继续部署
